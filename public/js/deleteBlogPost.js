const deleteBlogPost = async (event) => {
  event.preventDefault();

  const title = document.querySelector("").value;
  const body = document.querySelector("").value;
  const blogpost_id = document.querySelector("");
};
